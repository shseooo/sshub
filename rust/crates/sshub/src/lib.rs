//! sshub 라이브러리 타깃 — examples/ 와 main.rs 가 공유.
//! 새 모듈은 여기 `pub mod` 로 추가한다 (여러 작업 흐름이 이 파일에 줄을 추가함 —
//! 기존 선언을 지우지 말 것).

pub mod i18n;
pub mod keymap;
pub mod session;
pub mod state;
pub mod theme;
